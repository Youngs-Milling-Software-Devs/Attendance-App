import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "space-between",
  },
  calendarContainer: {
    width: "100%",
    height: "100%",
    // borderWidth: 1,
    // borderColor: "blue",
  },
});

export { styles };
